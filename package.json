{
  "name": "calendar-task-manager",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Import calendars, auto-convert events to tasks, sync with live iCal feeds",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ical.js": "^2.2.1"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
